import styled from 'styled-components';
import StyledList from './default-list';

const FullPageList = styled(StyledList)`
  padding: 0 15%;
  
  li {
    margin-top: 20px;
    padding-bottom: 20px;
  }
`;

export default FullPageList;
