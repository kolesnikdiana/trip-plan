import styled from 'styled-components';

const Message = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  
  color: #FFF;
  font-weight: bold;
  font-size: 22px;
  line-height: 1.2em;
  text-align: center;
  text-shadow: 0 1px 4px #828282;
`;

export default Message;
